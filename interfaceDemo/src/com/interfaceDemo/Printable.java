package com.interfaceDemo;
/*
 * through interface we can approach multiple inheritance
 * public by default
 * class implement interface but on interface extends another interface
 * 
 * */

public interface Printable {
	int number = 45;// public static final: Global constant

	void display();// adds abstract by the compiler

}
